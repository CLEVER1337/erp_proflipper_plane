{
  "_comment": "Plane NON-SECRET config, committed. Loaded into env by settings/common.py. Secrets (SECRET_KEY, ERP_GATEWAY_TOKEN, INTEGRATION_API_KEY, DB_*) come from env — NOT here (public repo). Real env overrides any value here.",

  "DEBUG": "0",
  "PORT": "8000",
  "GUNICORN_WORKERS": "2",
  "WEB_URL": "http://localhost:8000",
  "CORS_ALLOWED_ORIGINS": "http://localhost:3000",

  "CELERY_TASK_ALWAYS_EAGER": "1",
  "USE_MINIO": "0",
  "AWS_S3_ENDPOINT_URL": "",
  "_comment_rate_limit": "The whole ERP talks to this API through one gateway bot token, so the limit is shared by every employee — a kanban board load alone is one request per column.",
  "API_KEY_RATE_LIMIT": "600/minute",

  "ERP_WORKSPACE_SLUG": "erp",
  "ERP_PROJECT_IDENTIFIER": "TASK",
  "ERP_PROJECT_NAME": "ERP Tasks",

  "USER_SERVICE_URL": "http://localhost:7237"
}
